= render 'shared/header'

.wrapper
  .new_form
    .new_messages
      .message あなたのお気に入りをアップグレードしよう

      = form_with model: @post,local: true do |f|
        = render partial: "shared/form",locals:{f: f,post: @post} 

= render partial: 'shared/footer'