= render partial: 'shared/header'

.wrapper
  .mid_wrapper
    .container
      -# サーチボックス
      = render partial: "shared/tags",locals: {tags: @tags}
          
      .main_container
        -# 雑貨一覧
        .posts_title Goods
        = render partial: "shared/index",locals: { posts: @posts}
        = paginate @posts

= render partial: 'shared/footer'