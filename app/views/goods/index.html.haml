= render partial: 'shared/header'

.wrapper
  .mid_wrapper
    .container
      .search_wrapper
        .search_container
          -# タグ一覧
          = render partial: "shared/tags",locals: {tag_cs: @tag_cs,tag_bs: @tag_bs,tag_gs: @tag_gs}
          
      .main_container
        -# 雑貨一覧
        .posts_title Goods
        = render partial: "shared/index",locals: { posts: @posts}
        = paginate @posts

= render partial: 'shared/footer'