-# 衣類の投稿に対しての「いいね」
- if post.class.name == "PostC"
  - if user_signed_in?
    - if post.liked_by?(current_user)
      = link_to post_c_like_c_path(post.id),method: :delete do
        %span= fa_icon "thumbs-up alredy_icon"
        = post.like_cs.count 
    - else
      = link_to post_c_like_c_path(post.id),method: :post do
        %span= fa_icon "thumbs-up yet_icon"
        = post.like_cs.count
  - else
    %p
      %span= fa_icon "thumbs-up"
      = post.like_cs.count

-# 本の投稿に対しての「いいね」
- if post.class.name == "PostB"
  - if user_signed_in?
    - if post.liked_by?(current_user)
      = link_to post_b_like_b_path(post.id),method: :delete do
        %span= fa_icon "thumbs-up alredy_icon"
        = post.like_bs.count 
    - else
      = link_to post_b_like_b_path(post.id),method: :post do
        %span= fa_icon "thumbs-up yet_icon"
        = post.like_bs.count
  - else
    %p
      %span= fa_icon "thumbs-up"
      = post.like_bs.count

-# 雑貨の投稿に対しての「いいね」
- if post.class.name == "PostG"
  - if user_signed_in?
    - if post.liked_by?(current_user)
      = link_to post_g_like_g_path(post.id),method: :delete do
        %span= fa_icon "thumbs-up alredy_icon"
        = post.like_gs.count 
    - else
      = link_to post_g_like_g_path(post.id),method: :post do
        %span= fa_icon "thumbs-up yet_icon"
        = post.like_gs.count
  - else
    %p
      %span= fa_icon "thumbs-up"
      = post.like_gs.count